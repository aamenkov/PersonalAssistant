# 018 — Перенос исправлений оплаты и UI после PR #14

Исправления перенесены в новую ветку от актуального `origin/master`, потому что исходный PR #14 был смержен до их отправки.

В состав вошли:

- явная регистрация новой `PaymentTransaction` как `Added`, чтобы EF Core выполнял `INSERT`;
- integration test состояния EF;
- кнопка `⏸ Отключить` в списке «Мои платежи»;
- исправление порядка выбора клавиатуры подтверждения добавления платежа.

Проверка выполнена перед публикацией PR.
