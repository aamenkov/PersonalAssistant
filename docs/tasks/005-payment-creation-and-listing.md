# Текущая задача

## Номер

005

## Этап

Создание и просмотр регулярных платежей.

## Ветка

`codex/005-payment-creation-and-listing`

## Цель

Реализовать первый полноценный пользовательский сценарий учета платежей через Telegram.

## Задачи этапа

- [x] Реализовать последовательное добавление платежа через `/add`.
- [x] Хранить состояние диалога в `ConversationState`.
- [x] Добавить подтверждение перед сохранением платежа.
- [x] Реализовать просмотр всех активных платежей через `/payments`.
- [x] Реализовать просмотр ближайших платежей через `/upcoming`.
- [x] Проверять принадлежность платежа текущему пользователю.
- [x] Добавить unit-тесты валидации и application-сценария.
- [x] Обновить README и документацию сценариев.
- [x] Запустить `dotnet build`, `dotnet test` и `docker compose config`.
- [x] Перенести этот файл в `docs/tasks/005-payment-creation-and-listing.md` перед Pull Request.

## Критерии готовности

- Пользователь может пройти сценарий `/add` после перезапуска приложения.
- В БД сохраняется только платеж текущего пользователя.
- `/payments` и `/upcoming` показывают понятный результат, включая отсутствие платежей.
- Сборка, тесты и Docker Compose проходят.
- Перед Pull Request `TASK.md` архивирован, а в корне его нет.

## Ограничения

Оплата, история транзакций, статистика и напоминания будут отдельными задачами. Способ оплаты, автосписание и комментарий добавим после базового сценария.

## Результат

Задача завершена. Добавлены `/add`, `/payments`, `/upcoming`, постоянное состояние диалога, application- и infrastructure-сервисы, фильтрация по владельцу и тесты.

Проверки: `dotnet build` — успешно, `dotnet test` — 9 unit-тестов успешно, `docker compose config` — успешно.
