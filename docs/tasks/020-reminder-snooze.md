# Задача 020. Отложенные напоминания

## Выполнено

- из `TODO.md` удален общий блок `MVP 1`;
- добавлена сущность `ReminderSnooze` и миграция `AddReminderSnooze`;
- snooze хранится отдельно от `DueDate` и не изменяет расписание платежа;
- добавлены варианты «Через час», «Сегодня вечером» и «Завтра»;
- время рассчитывается в локальном часовом поясе пользователя и сохраняется в UTC;
- в уведомление добавлена кнопка «Напомнить позже»;
- после окончания snooze отправляется отдельное напоминание;
- атомарное потребление snooze предотвращает повторную отправку несколькими worker-ами;
- добавлены тесты расчета времени snooze для UTC и часового пояса Москвы.

## Проверка

- `dotnet build PersonalAssistant.sln --no-restore`;
- `dotnet test PersonalAssistant.sln --no-restore` — 71 unit-тест и 1 integration test;
- `docker compose config` с тестовыми переменными;
- `git diff --check`.
